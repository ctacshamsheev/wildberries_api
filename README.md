# wildberries_api

ѕриложение wildberries_api предназначено дл€ хранени€ статистики продаж на маркетплейсе Wildberries. ќно взаимодействует
с публичным [API Wildberries](https://openapi.wb.ru/statistics/api/ru/), регул€рно загружа€ доступную статистику и
обеспечивает локальное хранение полученных
данных. ѕользователи могут просматривать сохраненную информацию, выполн€ть загрузку .xls файлов и использовать
аналитические инструменты дл€ более глубокого анализа данных о продажах.

÷ель:

ѕриложение wildberries_api направлено на предоставление пользовател€м (включа€ продавцов и аналитиков компаний) доступа
к данным о продажах на платформе Wildberries и обеспечение функционала дл€ анализа этих данных с целью оптимизации
стратегий продаж и повышени€ прибыльности.

÷елева€ аудитори€:

ѕриложение ориентировано на магазины и торговые площадки, де€тельность которых св€зана с маркетплейсом Wildberries. Ёто
включает владельцев бизнеса любого масштаба, торговых аналитиков, менеджеров по продажам и специалистов, ответственных
за анализ и управление продажами в компани€х.

## «апуск приложени€

в `docker-compose.yml` необходимо указать свой токен
```WILDBERRIES_STATISTICS_AUTH_TOKEN: 'auth_token'```
после выполнить команду в корне папки проекта

````bash
docker-compose up  
````

## ѕересборка приложени€:

из папки  ```cd wildberries-api-statistics```
выполнить ```mvn clean install```

в `docker-compose.yml`

либо расскоментировать `build:` и использовать локальную версию контейнера,
и выполнить запуск `docker-compose up`

либо `image:`
и тогда необходимо обновить файл контейнера в репозитории:

`docker build -t ctacshamsheev/wildberries-api-statistics:v1.0 .`
`docker push ctacshamsheev/wildberries-api-statistics:v1.0`

(необходимо указать свой docker репозиторий, при загрузке и в `docker-compose.yml`)

